import React from "react";
import styles from "./CenterAdd.css";

export default class CenterAdd extends React.Component {
  render() {
    return (
      <div className={styles.base}>
        <div className={styles.textHolder}>M</div>
      </div>
    );
  }
}
