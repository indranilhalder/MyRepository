machine:
  node:
    version: 8.2.0
dependencies:
  pre:
    - node --version
    - npm --version
  override:
    - yarn install
test:
  override:
    - yarn  lint
    - yarn  test 
deployment:
  staging:
    branch: master
    commands:
      - yarn deploy    
